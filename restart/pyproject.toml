[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "restart"
version = "1.0.0"
description = "A configuration-driven generic restart daemon"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
restart-daemon = "restart.daemon:main"
